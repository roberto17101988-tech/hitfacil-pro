import { Sparkles, Flame, Mic, Library } from 'lucide-react';
import type { Page } from '@/lib/types';
interface BottomNavProps { active: Page; onChange: (page: Page) => void; }
const navItems: { id: Page; label: string; icon: typeof Sparkles }[] = [{ id: 'create', label: 'Criar', icon: Sparkles }, { id: 'explore', label: 'Em alta', icon: Flame }, { id: 'cover', label: 'Cover', icon: Mic }, { id: 'library', label: 'Biblioteca', icon: Library }];
export function BottomNav({ active, onChange }: BottomNavProps) { return <nav className="fixed bottom-0 left-0 right-0 z-40 border-t border-white/8 bg-[#080b09]/95 backdrop-blur-xl"><div className="mx-auto flex max-w-md items-center justify-around px-2 py-2.5">{navItems.map(({ id, label, icon: Icon }) => { const selected = active === id; return <button key={id} onClick={() => onChange(id)} className="flex flex-col items-center gap-1 rounded-xl px-3 py-1.5"><Icon className={`h-5 w-5 transition-all ${selected ? 'text-lime-300' : 'text-white/35'}`} /><span className={`text-[10px] font-semibold ${selected ? 'text-lime-300' : 'text-white/35'}`}>{label}</span>{selected && <span className="h-1 w-1 rounded-full bg-lime-300" />}</button>; })}</div></nav>; }
